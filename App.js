let sum = 0;
for (let i = 0; i < 100 ; i++) {
   console.log (i); 
}

let w = 0;
while (w < 50) {
    i++;
    console.log (w);
}

let person = ["salome", "shako", "diana", "giorgi", "damiane"];
person.pop();
person.push("irakli");
person.shift();
person.unshift("mariami");
console.log (person);

let numbers = [];
for (let index = 1; index < 1000; length; index++) {
    numbers.push(index * index);
}
console.log(numbers);

const names = ["salome","shako"];
for (let index = 0; index < names.length; index++) {
    if (names[index].length > 2) {
      const element = names[index].toUpperCase(); 
    }
    console.log(element);
else {
    console.log("wrong act");
}
}